# ============================================================
# docker-compose.override.yml — Development overrides
# Automatically loaded by `docker-compose up` (no -f flag needed).
# ============================================================

services:
  api:
    environment:
      - APP_ENV=development
      - CORS_ORIGINS=http://localhost:3000,http://localhost:5173
      - LOG_REQUESTS=true
