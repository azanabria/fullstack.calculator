# ============================================================
# Frontend — Production environment
# Loaded automatically by Vite when running `npm run build`
# ============================================================

# API base URL (empty = same origin, nginx proxies /api → backend)
VITE_API_URL=

# Environment label
VITE_APP_ENV=production
