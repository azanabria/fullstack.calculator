# ============================================================
# Frontend — Staging environment
# Loaded by Vite when running with --mode staging
# Usage: npm run build -- --mode staging
# ============================================================

# API base URL — points to the staging backend
VITE_API_URL=https://api-staging.example.com

# Environment label
VITE_APP_ENV=staging
