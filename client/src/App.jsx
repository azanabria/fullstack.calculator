/* ============================================================
   APP SHELL — src/App.jsx
   Root component that renders the calculator.
   ============================================================ */

import React from 'react';
import './App.css';
import Calculator from './components/Calculator';

export default function App() {
  return <Calculator />;
}
